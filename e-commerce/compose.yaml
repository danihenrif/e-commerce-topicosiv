services:
  buyservice:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: buy-service
    ports:
      - "8080:8080"
    networks:
      - app-network
    volumes:
      - ./logs:/app/logs
networks:
  app-network:
    driver: bridge
